@Schema NekoSquare {
    @Table Accounts {
        @Primary @String email_address;
	    @String password;
    }
    @Table Tokens {
	@Primary @Depends(Accounts.email_address, "fk_accounts") @String email_address;
	@String token;
    }
}

